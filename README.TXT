# comandos git:
git add . 
git commit -m "Nombrar evento o accion"
git push origin main
# dependencias :
visuales del git : pip install customtkinter(el tkinter pero pro) -
pillow(visualizador imagenes)
# para instalar estas dependencias incertar estos comandos (mejor si es en una maquina virtual)
(opcional) python3 -m venv venv        # crean el entorno virtual para no romper visual 
(opcional) source venv/bin/activate    # activan el entorno 
pip install -r requirements.txt  # librerias que se usaron pillow y customtkinder
# BITACORA Proyecto https://docs.google.com/document/d/1Nlcs_m1fBlIizSWNfEaoDFsGtzcq6g96oV9r9JpxVYc/edit?usp=sharing
